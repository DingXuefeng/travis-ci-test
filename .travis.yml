language: cpp
jobs:
  include:
    - os: linux
      dist: trusty
      compiler: gcc
    - os: osx
      osx_image: xcode11.3.1
      compiler: clang
script:
  - mkdir build; cd build; cmake ..
  - cmake --build .
